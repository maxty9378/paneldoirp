# Скачивание и установка сертификата от reg.ru

## 📋 Информация о сертификате

Ваш сертификат:
- **Имя:** doirp
- **Статус:** Issued (выдан) ✅
- **Тип:** Imported
- **Действителен до:** 21.06.2026
- **Домены:** doirp.ru, www.doirp.ru, autodiscover.doirp.ru, mail.doirp.ru, owa.doirp.ru
- **Издатель:** GlobalSign GCC R3 DV TLS CA 2020

## 🔽 Шаг 1: Скачивание сертификата из панели reg.ru

1. Войдите в панель управления reg.ru
2. Перейдите в раздел **SSL сертификаты** или **Безопасность**
3. Найдите сертификат **doirp** (идентификатор: fpq8lgddl25d3jkv1fph)
4. Нажмите **Скачать** или **Экспорт**
5. Скачайте файлы:
   - **Сертификат** (обычно `doirp.crt` или `doirp.pem`)
   - **Приватный ключ** (обычно `doirp.key`) - **у вас уже есть!**
   - **Цепочка сертификатов** (если есть, обычно `chain.crt` или `ca-bundle.crt`)

### Если есть цепочка сертификатов:

Объедините основной сертификат с цепочкой:
```bash
# На вашем компьютере
cat doirp.crt chain.crt > doirp.fullchain.crt
```

## 📤 Шаг 2: Загрузка файлов на сервер

### Вариант А: Через PowerShell (Windows)

```powershell
# Загрузите сертификат
scp -i src/ssh/ssh-key-doirp-01 путь/к/doirp.crt doirp@51.250.94.103:/tmp/doirp.ru.crt

# Загрузите приватный ключ (если еще не загружен)
scp -i src/ssh/ssh-key-doirp-01 doirp.ru.key doirp@51.250.94.103:/tmp/doirp.ru.key

# Если есть цепочка сертификатов:
scp -i src/ssh/ssh-key-doirp-01 путь/к/chain.crt doirp@51.250.94.103:/tmp/doirp.ru.chain.crt

# Или если уже объединили:
scp -i src/ssh/ssh-key-doirp-01 doirp.fullchain.crt doirp@51.250.94.103:/tmp/doirp.ru.fullchain.crt
```

### Вариант Б: Сохранение приватного ключа (если еще не сохранен)

Если у вас еще нет файла `doirp.ru.key`:

1. Создайте файл `doirp.ru.key` на вашем компьютере
2. Вставьте ваш приватный ключ (весь блок):
```
-----BEGIN RSA PRIVATE KEY-----
...ваш ключ...
-----END RSA PRIVATE KEY-----
```

3. Сохраните файл
4. Загрузите на сервер (см. выше)

## 🔧 Шаг 3: Установка на сервере

### Автоматическая установка (рекомендуется)

1. Загрузите скрипт на сервер:
```powershell
scp -i src/ssh/ssh-key-doirp-01 install-regru-cert.sh doirp@51.250.94.103:~/
```

2. Подключитесь к серверу и запустите:
```bash
ssh -l doirp 51.250.94.103
chmod +x install-regru-cert.sh
sudo bash install-regru-cert.sh
```

### Ручная установка

Подключитесь к серверу:
```bash
ssh -l doirp 51.250.94.103
```

Выполните команды:
```bash
# Создаем директорию для Docker контейнера
sudo mkdir -p /etc/ssl/certs/doirp.ru

# Если есть готовый fullchain сертификат:
sudo cp /tmp/doirp.ru.fullchain.crt /etc/ssl/certs/doirp.ru/fullchain.pem

# Или если нужно объединить сертификат с цепочкой:
if [ -f /tmp/doirp.ru.chain.crt ]; then
    sudo cat /tmp/doirp.ru.crt /tmp/doirp.ru.chain.crt > /etc/ssl/certs/doirp.ru/fullchain.pem
else
    sudo cp /tmp/doirp.ru.crt /etc/ssl/certs/doirp.ru/fullchain.pem
fi

# Копируем приватный ключ
sudo cp /tmp/doirp.ru.key /etc/ssl/certs/doirp.ru/privkey.pem

# Устанавливаем права доступа
sudo chmod 644 /etc/ssl/certs/doirp.ru/fullchain.pem
sudo chmod 600 /etc/ssl/certs/doirp.ru/privkey.pem
sudo chown root:root /etc/ssl/certs/doirp.ru/*

# Проверяем
sudo ls -la /etc/ssl/certs/doirp.ru/
```

## 🚀 Шаг 4: Настройка SSL

После установки сертификата запустите скрипт настройки:

```bash
# Загрузите скрипт на сервер (с локальной машины)
scp -i src/ssh/ssh-key-doirp-01 setup-domain-ssl.sh doirp@51.250.94.103:~/

# На сервере запустите:
chmod +x setup-domain-ssl.sh
./setup-domain-ssl.sh
```

Скрипт автоматически обнаружит готовый сертификат и использует его.

## ✅ Шаг 5: Проверка

После установки проверьте:

```bash
# Проверьте наличие файлов
sudo ls -la /etc/ssl/certs/doirp.ru/

# Проверьте сертификат
openssl x509 -in /etc/ssl/certs/doirp.ru/fullchain.pem -text -noout | head -20

# Проверьте сайт
curl -I https://doirp.ru
curl -I https://www.doirp.ru
```

В браузере откройте:
- https://doirp.ru
- https://www.doirp.ru

## 📍 Итого: путь к файлам на сервере

Для Docker контейнера (используется в setup-domain-ssl.sh):
- **Сертификат:** `/etc/ssl/certs/doirp.ru/fullchain.pem`
- **Ключ:** `/etc/ssl/certs/doirp.ru/privkey.pem`

## ⚠️ Важно

1. **Не потеряйте приватный ключ!** Без него вы не сможете использовать сертификат.
2. **Права доступа:**
   - Сертификат: `644` (читаемый)
   - Ключ: `600` (только владелец)
3. **Срок действия:** Сертификат действителен до 21.06.2026. Не забудьте обновить его до этой даты.

## 🔄 Если что-то пошло не так

1. Проверьте логи Docker контейнера:
```bash
sudo docker logs doirp-app
```

2. Проверьте права доступа:
```bash
sudo ls -la /etc/ssl/certs/doirp.ru/
```

3. Убедитесь, что файлы не пустые:
```bash
sudo cat /etc/ssl/certs/doirp.ru/fullchain.pem
sudo cat /etc/ssl/certs/doirp.ru/privkey.pem
```

4. Проверьте, что порты 80 и 443 открыты в Yandex Cloud Firewall

