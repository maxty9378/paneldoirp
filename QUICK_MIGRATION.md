# Быстрая миграция на Яндекс.Облако

## Краткая инструкция

### 1. Подключитесь к ВМ
```bash
ssh -l doirp 51.250.94.103
```

### 2. Установите Supabase
```bash
# Скопируйте скрипт на ВМ
# На локальном компьютере:
scp deploy-supabase-yandex.sh doirp@51.250.94.103:~/

# На ВМ:
chmod +x deploy-supabase-yandex.sh
./deploy-supabase-yandex.sh
```

### 3. Настройте файрвол
```bash
# На локальном компьютере (если установлен YC CLI):
chmod +x setup-yandex-firewall.sh
./setup-yandex-firewall.sh

# Или вручную через веб-консоль Яндекс.Облако:
# VPC → Группы безопасности → default-sg-enphq6fifa9dqk9eo7gd
# Добавьте правила для портов: 8000, 54323, 443
```

### 4. Мигрируйте данные
```bash
# Экспорт с текущего Supabase
supabase db dump -f backup.sql --project-ref oaockmesooydvausfoca

# Импорт на новый сервер
scp backup.sql doirp@51.250.94.103:~/
ssh -l doirp 51.250.94.103
cd ~/supabase
psql -h localhost -U postgres -d postgres -f ~/backup.sql
```

### 5. Получите ключи
```bash
# На ВМ
cd ~/supabase
cat .env
# Скопируйте ANON_KEY
```

### 6. Обновите приложение
Создайте `.env.production.local`:
```env
VITE_SUPABASE_URL=http://51.250.94.103:8000
VITE_SUPABASE_ANON_KEY=ваш_anon_key
```

Пересоберите:
```bash
npm run build
```

### 7. Проверьте работу
- Откройте приложение
- Попробуйте авторизоваться
- Проверьте на iPhone с МТС

## Важные моменты

⚠️ **Безопасность:**
- Измените пароли в `.env` файле
- Ограничьте доступ к Studio (порт 54323) только вашим IP
- Настройте SSL сертификат для production

📊 **Мониторинг:**
- Проверяйте использование ресурсов: `htop`
- Настройте резервное копирование
- Мониторьте логи: `supabase logs`

🔧 **Оптимизация:**
- При необходимости увеличьте ресурсы ВМ
- Настройте домен вместо IP
- Используйте CDN для статики

