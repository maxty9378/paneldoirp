# Финальные шаги миграции на Яндекс.Облако

## Текущий статус

✅ Supabase CLI установлен на ВМ  
✅ Проект инициализирован  
🔄 Supabase запускается...

## Шаг 1: Проверка запуска Supabase

```bash
ssh -i src/ssh/ssh-key-doirp-01 doirp@51.250.94.103
cd ~/supabase
supabase status
```

Должны увидеть:
- API URL: http://51.250.94.103:8000
- Studio URL: http://51.250.94.103:54323
- Все сервисы в статусе "Running"

## Шаг 2: Получение ключей

```bash
cat ~/supabase/.env | grep -E "(ANON_KEY|SERVICE_ROLE_KEY)"
```

Скопируйте значения для использования в приложении.

## Шаг 3: Настройка файрвола

См. файл `FIREWALL_SETUP.md` для подробной инструкции.

Кратко:
1. Яндекс.Облако → VPC → Группы безопасности → `default-sg-enphq6fifa9dqk9eo7gd`
2. Добавьте правила для портов: 8000, 54323, 443

## Шаг 4: Миграция данных

### 4.1. Экспорт из текущего Supabase

Используйте pg_dump напрямую или через Supabase CLI:

```bash
# Получите connection string из Supabase Dashboard
# Settings → Database → Connection string (URI)

pg_dump "postgresql://postgres:[PASSWORD]@db.oaockmesooydvausfoca.supabase.co:5432/postgres" > backup.sql
```

### 4.2. Импорт на новый сервер

```bash
ssh -i src/ssh/ssh-key-doirp-01 doirp@51.250.94.103
cd ~/supabase

# Загрузите backup.sql на ВМ
# Затем выполните:
psql -h localhost -U postgres -d postgres -f backup.sql
```

Или через Supabase CLI:
```bash
supabase db reset
# Затем импортируйте данные
```

## Шаг 5: Обновление конфигурации приложения

Создайте `.env.production.local`:

```env
VITE_SUPABASE_URL=http://51.250.94.103:8000
VITE_SUPABASE_ANON_KEY=ваш_anon_key_из_шага_2
```

## Шаг 6: Пересборка и тестирование

```bash
npm run build
```

Протестируйте:
- Авторизацию
- Загрузку данных
- На iPhone с МТС

## Шаг 7: Настройка домена (опционально)

Если есть домен:
1. Настройте A-запись на 51.250.94.103
2. Установите Nginx и SSL (Let's Encrypt)
3. Обновите `VITE_SUPABASE_URL` на домен

## Проблемы и решения

### Supabase не запускается
- Проверьте логи: `supabase logs`
- Проверьте Docker: `docker ps`
- Проверьте ресурсы: `free -h`, `df -h`

### Не могу подключиться к API
- Проверьте файрвол
- Проверьте, что Supabase запущен: `supabase status`
- Проверьте порты: `netstat -tlnp | grep 8000`

### Ошибки миграции данных
- Проверьте версию PostgreSQL (должна быть 15)
- Проверьте размер backup.sql
- Импортируйте по частям при необходимости

